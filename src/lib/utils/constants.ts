const constants = {
  API_URL: import.meta.env.VITE_API_URL,
  API_SEND_TIMEOUT: import.meta.env.VITE_API_TIMEOUT,
  RXDB_DB_NAME: import.meta.env.VITE_RXDB_DB_NAME,
  AUTH_TOKEN: "AUTH_TOKEN",
  REF_TOKEN: "REF_TOKEN",
  WORKSPACE_LIMIT: 5,
};

export default constants;
